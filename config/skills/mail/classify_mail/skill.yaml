# Capability delivered to the Mail Agent.
tool_name: classify_mail
implementation: mail.classify

# The model selects tools from this text, so it is part of the contract.
description: >-
  Assign one business category to each of the given messages, with a confidence and a short
  reason. Has no side effect and does not apply any label to the mailbox.

# Security posture. The risk floor enforced in code cannot be lowered here.
operation:
  type: READ
  risk: LOW
  permission: mail:read
  confirmation_required: false

# Logical MCP tools this capability is allowed to use.
mcp_tools:
  - get_mail
